<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springbootredis.dao.User2Dao">
    <resultMap id="BaseResultMap" type="com.springbootredis.model.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="role_id" property="roleId" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="top" property="top" jdbcType="BIT"/>
        <result column="userType" property="userType" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, role_id, username, password, age, top, userType
    </sql>
    <select id="findByType" parameterType="java.lang.String" resultMap="BaseResultMap" >
        select
        <include refid="Base_Column_List"/>
        from user
        <where>
            <if test="state != null">
                userType = #{state}
            </if>
        </where>
    </select>

    <insert id="add" parameterType="com.springbootredis.model.User">
        insert into user (id,role_id,username, password, age, top, userType) values
        (#{id},#{roleId},#{username}, #{password}, #{age}, #{top}, #{userType})
    </insert>
</mapper>